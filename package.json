{
  "name": "fenwick-recorder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node tests/background.test.mjs",
    "check": "node --check background.js && node --check offscreen.js && node --check popup.js && node --check auth.js && node --check options.js"
  }
}
